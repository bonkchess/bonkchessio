import React from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";

export default function SolanaChessHome() {
  return (
    <div className="min-h-screen bg-gray-100 p-6">
      <header className="text-center mb-10">
        <h1 className="text-4xl font-bold mb-2">SolanaChess</h1>
        <p className="text-lg text-gray-700">Play chess on the blockchain. Fast, decentralized, and secure with Solana.</p>
      </header>

      <Tabs defaultValue="play" className="max-w-4xl mx-auto">
        <TabsList className="grid w-full grid-cols-3 mb-6">
          <TabsTrigger value="play">Play</TabsTrigger>
          <TabsTrigger value="puzzles">Puzzles</TabsTrigger>
          <TabsTrigger value="profile">Profile</TabsTrigger>
        </TabsList>

        <TabsContent value="play">
          <Card>
            <CardContent className="p-6 space-y-4">
              <h2 className="text-2xl font-semibold">Find a Match</h2>
              <div className="flex gap-4">
                <Button className="w-full">Quick Match</Button>
                <Button className="w-full" variant="outline">Create Game</Button>
              </div>
              <p className="text-sm text-gray-500">All games are stored immutably on the Solana blockchain.</p>
            </CardContent>
          </Card>
        </TabsContent>

        <TabsContent value="puzzles">
          <Card>
            <CardContent className="p-6 space-y-4">
              <h2 className="text-2xl font-semibold">Daily Puzzles</h2>
              <p className="text-gray-700">Sharpen your tactics with blockchain-backed puzzles. Earn SOL for solving them!</p>
              <Button>Start Puzzle</Button>
            </CardContent>
          </Card>
        </TabsContent>

        <TabsContent value="profile">
          <Card>
            <CardContent className="p-6 space-y-4">
              <h2 className="text-2xl font-semibold">Connect Wallet</h2>
              <p className="text-gray-700">Connect your Solana wallet to view your games, puzzles, and leaderboard ranking.</p>
              <Input placeholder="Enter Wallet Address" />
              <Button>Connect</Button>
            </CardContent>
          </Card>
        </TabsContent>
      </Tabs>
    </div>
  );
}
